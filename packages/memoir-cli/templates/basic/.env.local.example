# Memoir SDK - Configuration
# Copy this file to .env.local and fill in your values

# Required: Your Convex deployment URL
CONVEX_URL=https://your-deployment.convex.cloud

# Optional: OpenAI API key for LLM responses and embeddings
# If not set, the agent runs in "echo mode" (returns user message as response)
OPENAI_API_KEY=sk-...

# Memory configuration
MEMORY_SPACE_ID=basic-demo
USER_ID=demo-user
USER_NAME=Demo User
AGENT_ID=basic-assistant
AGENT_NAME=Memoir CLI Assistant

# Feature flags
MEMOIR_FACT_EXTRACTION=true
MEMOIR_GRAPH_SYNC=false

# Debug mode (enables verbose logging)
DEBUG=false
